# Dart / Flutter build artifacts
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# Workspace lockfile (CI runs `pub get`)
pubspec.lock

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store

# NOTE: generated Drift code (lib/src/*.g.dart) IS committed on purpose so the
# published package and consumers don't need build_runner.
